set(autostart ${PLUGIN_RBUSDIAGNOSTICS_AUTOSTART})
set(callsign "org.rdk.RBusDiagnostics")

if(PLUGIN_RBUSDIAGNOSTICS_STARTUPORDER)
    set(startuporder ${PLUGIN_RBUSDIAGNOSTICS_STARTUPORDER})
endif()
